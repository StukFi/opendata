import { shallowMount } from "@vue/test-utils"
import FeaturePopup from "./FeaturePopup"

xdescribe("FeaturePopup.vue", () => {
    let wrapper

    beforeEach(() => {
        wrapper = shallowMount(FeaturePopup)
    })

    it("closes when the close button is clicked", async () => {
    })

    it("opens when a feature is clicked", async () => {
    })

    it("opens when a feature is found via search", async () => {
    })

    it("updates when the dose rate layer changes", async () => {
    })
})
